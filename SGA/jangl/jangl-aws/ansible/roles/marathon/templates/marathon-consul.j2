{
  "service": {
    "name": "marathon",
    "tags": [ "marathon" ],
    "port": 8080,
    "check": {
      "args": ["curl", "--silent", "--show-error", "--fail", "--dump-header", "/dev/stderr", "--retry", "2", "--connect-timeout", "5", "--max-time", "5", "http://127.0.0.1:8080/ping"],
      "interval": "10s",
      "timeout": "2s"
    }
  }
}
