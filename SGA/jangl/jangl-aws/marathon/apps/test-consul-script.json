{
  "id": "test-consul-script",
  "cmd": "set -x;TASK_NAME=${MARATHON_APP_LABEL_OVERRIDETASKNAME:-${MESOS_TASK_ID%%.*}};echo mesos-consul:${HOST}:${TASK_NAME}:${HOST}:${PORT0}; env | sort;apt-get update && apt-get install -y curl && curl -X DELETE http://marathon.service.consul:8080/v2/apps${MARATHON_APP_ID}; sleep 10",
  "container": {
    "type": "DOCKER",
    "docker": {
      "image": "python:2.7-slim",
      "forcePullImage": true
    }
  },
  "labels": {
      "tags": "test",
      "overrideTaskName": "test-script"
  },
  "instances": 1,
  "cpus": 0.75,
  "mem": 1024
}
